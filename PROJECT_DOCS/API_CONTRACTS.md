# API Contracts

> BE Agent cập nhật sau mỗi endpoint mới. FE Agent đọc trước khi implement.

---

## Template

```
### [METHOD] /api/v1/[resource]
Auth: Bearer required / Public
Request: {field: type}
Response [status]: {field: type}
Response error: {error: "CODE", message: "..."}
```

---

## Endpoints

(BE Agent điền vào đây)
